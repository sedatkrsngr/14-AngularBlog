export class Archive {
  year: number;
  month: number;
  count: number;
  monthName: string;
  constructor(year: number, month: number, count: number, monthName: string) {
  this.year=year;
  this.month=month;
  this.count=count;
  this.monthName=monthName;
  }
}
